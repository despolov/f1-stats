const getStyles = () => ({
  mainContainer: {
    position: 'relative',
    display: 'flex',
    flexFlow: 'column wrap',
    flexGrow: 1,
    minHeight: '100vh',
    maxWidth: '100%',
  },
});

export default getStyles;
